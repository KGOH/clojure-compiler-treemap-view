name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

permissions:
  contents: write

concurrency:
  group: "ci-${{ github.ref }}"
  cancel-in-progress: true

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Setup Java
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '21'
      - name: Setup Clojure
        uses: DeLaGuardo/setup-clojure@12.5
        with:
          cli: 'latest'
      - name: Cache Clojure dependencies
        uses: actions/cache@v4
        with:
          path: |
            ~/.m2/repository
            ~/.gitlibs
            ~/.clojure
          key: ${{ runner.os }}-clojure-${{ hashFiles('**/deps.edn') }}
          restore-keys: |
            ${{ runner.os }}-clojure-
      - name: Build metrics agent
        run: mvn -f metrics-agent/pom.xml package -q
      - name: Run tests
        run: clojure -M:agent:test:runner

  update-metrics:
    needs: test
    if: github.ref == 'refs/heads/main' && github.event_name == 'push'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Setup Java
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '21'
      - name: Setup Clojure
        uses: DeLaGuardo/setup-clojure@12.5
        with:
          cli: 'latest'
      - name: Cache Clojure dependencies
        uses: actions/cache@v4
        with:
          path: |
            ~/.m2/repository
            ~/.gitlibs
            ~/.clojure
          key: ${{ runner.os }}-clojure-${{ hashFiles('**/deps.edn') }}
          restore-keys: |
            ${{ runner.os }}-clojure-
      - name: Build metrics agent
        run: mvn -f metrics-agent/pom.xml package -q
      - name: Generate metrics
        run: |
          clojure -M:agent:generate-metrics
          mv metrics.json metrics.json.new
      - name: Commit metrics if changed
        run: |
          # Compare ignoring timestamp
          if [ -f metrics.json ]; then
            OLD_DATA=$(jq -S 'del(.generated)' metrics.json)
            NEW_DATA=$(jq -S 'del(.generated)' metrics.json.new)
            if [ "$OLD_DATA" = "$NEW_DATA" ]; then
              echo "Metrics unchanged, skipping commit"
              rm metrics.json.new
              exit 0
            fi
          fi

          mv metrics.json.new metrics.json

          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add metrics.json
          git commit -m "Update metrics.json [skip ci]"
          git push
